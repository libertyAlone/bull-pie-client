# MCP 用法：让你的 AI 直接查本机的行情与财务

牛派内置一个 **MCP（Model Context Protocol）服务器**：`bull-pie-cli.exe mcp`。把它接到支持 MCP 的
AI 客户端（Claude Desktop、Cursor、Cline、Continue、Windsurf、Zed…）之后，模型就能**自己调用**
本软件的数据能力——查行情、看财务、跑公式选股、跑回测、看期货期权与基金，不用你手动复制粘贴数字。

它就是随包附带的命令行程序：**只读**、**只在本机跑**，不会改你的数据、也不会替你下单。

---

## 一、什么场景值得接

| 场景 | 用起来是什么样 |
| --- | --- |
| **边聊边查**：看盘时不想来回切窗口 | 「600519 现在多少钱、量比多少？」「它最近三期营收和净利润的趋势如何？」——模型直接调 `quote_snapshot` / `financials`，回答下方会列出用了哪个工具、什么参数 |
| **多步研究**：一句话串好几步 | 「找几只和 600519 走势像、最近底部放量的票，再看其中成交额最大的那只的财务」——模型自己排 `shape_search` → `financials` |
| **把自然语言变成可复现的条件** | 「把『5 日均线上穿 20 日均线且成交量放大 1.5 倍』写成通达信公式，再用它扫全市场」——`formula_check` 校验语法、`screen_formula` 在本地底库上出名单 |
| **验证一个想法值不值得用** | 「这条公式命中后 20 天的表现怎么样？」——`signal_study` 给出命中后收益、超额、t 值与分年度稳定性 |
| **写作 / 复盘时现取数据** | 「今天涨停梯队里三板以上的票，按封单额排序」——`market_sentiment` / `limit_up_ladder` |
| **不想为取数写脚本** | 想自己写脚本用命令行，见 [cli-intro.md](cli-intro.md)；只是想让 AI 帮你取数就用 MCP |

**不适合**：想让云端网页版 AI 直连你电脑（MCP 走 stdio，只能由本机进程启动，见文末「限制」）；
想要自动下单（本软件不接券商交易通道）。

---

## 二、前置条件

1. 装好牛派（安装版或免安装版都行）。免安装版的 `bull-pie.exe` 与 `bull-pie-cli.exe` 在同一目录。
2. **先用图形界面配一次数据接口凭据**（设置 → API 凭据）。MCP 读的是同一份本机凭据（系统凭据库），
   所以不必每次给客户端填 Key；也可以用环境变量注入（见下）。
3. **图形界面不必一直开着**：MCP 是独立进程，读同一份数据目录。但本地库越全，能回答的问题越多——
   想跑全市场选股 / 回测，先在「设置 → 数据」导入全市场日线底库。

> 没有凭据时仍可用：`data_status`、`formula_check`、`formula_library`、`screen_formula`、
> `shape_search`、`backtest_run` / `backtest_list`、`dump_status`（都只读本地库）。
> `flash_news`、`global_index`、`global_index_bars`、`stock_f10_ext` 只要外网能通、**不需要**同花顺凭据。

---

## 三、接上客户端（三分钟）

找到 `bull-pie-cli.exe` 的完整路径（例如 `C:\Tools\bull-pie\bull-pie-cli.exe`），在客户端配置里加一段：

**Claude Desktop**（`claude_desktop_config.json`）、**Cursor**（`mcp.json`）、**Cline / Continue / Windsurf** 结构一致：

```json
{
  "mcpServers": {
    "bull-pie": {
      "command": "C:/Tools/bull-pie/bull-pie-cli.exe",
      "args": ["mcp"]
    }
  }
}
```

- Windows 路径用**正斜杠** `/`，或用双反斜杠 `\\`（JSON 里单个 `\` 是转义符）。
- 数据目录不在默认位置时加 `--app-dir`：`"args": ["mcp", "--app-dir", "D:/bull-pie-data"]`。
- 想用环境变量给凭据（不用界面里的凭据库）：加 `"env": { "HITHINK_FINANCE_API_KEY": "你的凭据" }`。
  凭据只存在你机器上，软件不会上传。
- **开发期**不打包也能用：`"command": "npm", "args": ["run", "cli", "--", "mcp"]`（首次会编译，较慢）。

改完配置重启客户端，先问一句「本地数据状态怎么样」——能列出库路径、行数、最近交易日，就说明接通了。

---

## 四、能用哪些能力（44 个只读工具）

| 分组 | 工具 |
| --- | --- |
| 本地与主线 | `data_status` 数据与凭据状态 · `search_ticker` 标的检索 · `daily_bars` K 线 · `indicator_series` 内置指标 |
| 公式与研究 | `formula_check` 语法自检 · `formula_library` 公式库 · `screen_formula` 公式选股 · **`formula_signals` 公式买卖点**（与 K 线上的 B/S 同源） · `shape_search` 形态相似 · `signal_study` 信号体检 |
| 回测 | `backtest_run` 跑回测 · `backtest_list` 历史记录 · **`backtest_get` 读某条记录的完整结果**（净值曲线等距抽样 + 成交明细） |
| 行情与市场 | `quote_snapshot` 个股行情 · `index_snapshot` 指数/板块行情 · `index_bars` 指数/板块日线 · `market_breadth` 市场宽度 · `market_sentiment` 市场情绪 · `market_ranking` 全市场排名 · `sector_ranking` 板块榜 · `sector_detail` 板块成分股 · `dragon_tiger` 龙虎榜 · `limit_up_ladder` 连板天梯 · `auction` 集合竞价 |
| 个股基本面 | `valuations` 估值（PE/PB/PS/PCF） · `financials` 三表与五类指标 · `stock_f10` F10 · `stock_f10_ext` 资金流/股东户数/公告（第三方源） |
| 期货与期权 | `futures_varieties` 品种 · `futures_contract` 合约/主连日线 · `futures_basis` 基差 · `futures_positions` 持仓 · `futures_warehouse` 仓单 · `option_varieties` 期权品种 · `option_contract` 期权合约 |
| 基金 | `fund_detail` 详情 · `fund_nav` 净值 · `fund_bars` 场内日线（仅 ETF） · `fund_financials` 财务 · `fund_news` 资讯 |
| 第三方展示源 | `flash_news` 7×24 快讯（东方财富） · `global_index` 全球指数 · `global_index_bars` 指数日线 |
| 本地数据 | `dump_status` 全市场底库状态（导入动作只能在界面里做） |

第三方源（东方财富 / 巨潮资讯）在工具描述里都点名了，**只作阅读**，不进回测、选股、底库与推送链路。

---

## 五、怎么问才准

分工是「模型挑工具、本机出数据」，问法注意三点：

1. **代码带后缀**（`600519.SH` / `000001.SZ` / `885431.TI`）。只说名字也行——模型会先调
   `search_ticker` 消歧，但直接给代码少一轮。
2. **一次问一件事**，多步问题说清顺序（默认最多 6 轮工具调用，用满后会强制收尾作答）。例如「先找相似形态，再取成交额最大的那只的财务」。
3. **要名单 / 排序时给范围和条数**：「全市场」「我的自选」「按成交额降序取前 20」。

几个可以直接抄的问法：

```
本地数据状态怎么样？底库导入了吗？
600519 现在多少钱？今天量比和换手率多少？
它最近三年（按年）的营业收入和净利润是多少？用表格给我
把「5 日均线上穿 20 日均线且成交量大于 5 日均量 1.5 倍」写成公式，然后用它扫全市场，排除 ST，取前 20
今天涨停梯队里三板以上的票有哪些？按封单额排序
找几只和 600519 最近 20 天走势相似、且近期放量的票，再看其中成交额最大的那只的估值
这条公式命中后 20 个交易日的历史表现如何？胜率和超额收益是多少？
沪铜主连（CUZL.SHF）最近 60 天的基差走势如何？今天是升水还是贴水？
用「MA5 上穿 MA20」当买点、「MA5 下穿 MA20」当卖点，标出 600519 最近半年的买卖点日期
把最近那条回测的净值曲线和成交明细给我看看，重点说最大回撤发生在什么时候
```

---

## 六、限制与注意

- **只有 stdio，没有 HTTP**：客户端必须能在本机启动进程。Claude / ChatGPT 的**网页版**不能直连，
  要么用桌面客户端（Claude Desktop、Cursor 等），要么自己加一个 MCP 代理桥。
- **只读**：没有任何写操作。加自选、存公式、建监控、改配置、导入底库都只能在图形界面里做
  （这是刻意的：MCP 的返回会被喂给大模型，不能让提示注入有可写的落点）。
- **结果会被截断**：目录类数据（期货品种、期权品种、基差、板块成分股、龙虎榜）默认只回前 30–60 行，
  K 线最多 200 根。返回里带 `total` / `returned` / `truncated`，需要更多就用各自的 `limit` 调大。
  模型应该照实说「我只看了前 N 条」——如果它没说，你可以反问一句。
- **数字只应来自工具返回**：本软件的约定是模型只负责挑工具与复述，不自己算平均 / 累计 / 换算单位。
  看到可疑数字，让它把用了哪个工具、什么参数说出来核对。
- **限流按进程算**：GUI 与 MCP 各自一把令牌桶，同时大批量拉数会互相抢配额。上游限流时会有明确提示。
- **第三方数据**：快讯、全球指数、资金流、股东户数、公告来自公开接口（东方财富 / 巨潮），
  未文档化、随时可能调整，只作展示，请遵守数据服务方的授权条款，不要二次分发。
- **不是投资建议**：所有数字都是历史数据的计算结果，据此操作风险自负。

---

## 七、排查

| 现象 | 原因与处理 |
| --- | --- |
| 客户端里看不到 `bull-pie` | 配置的路径不对（用绝对路径、正斜杠），或客户端没重启；确认 `bull-pie-cli.exe mcp` 能手动跑起来 |
| 工具报「未配置 API Key」 | 先在界面里配好凭据，或在客户端配置的 `env` 里给 `HITHINK_FINANCE_API_KEY` |
| 选股 / 回测返回空名单 | 本地底库没导入（先看 `dump_status`）；或在「设置 → 数据」导入全市场日线 |
| 问「今天涨停梯队」拿到昨天的 | 非交易日 / 盘中数据未就绪时，上游给的是最近一个交易日的快照，返回里会写明日期 |
| 结果被截断、模型说「只有这些」 | 让它带上更大的 `limit` 重问，或到界面里看完整列表 |
| 模型不调工具、直接编数字 | 换一个工具调用能力更好的模型（订阅版大模型通常明显更好；本地 7B 级模型经常不调） |
